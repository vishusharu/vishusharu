import java.util.Scanner;

public class Stringpro {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Scanner sc=new Scanner(System.in);
		 int	  inputs = 0;     
		 System.out.println("1=display the length of string\n"+
	                          "2=convert the string in lowercase formate\n"+
	    		              "3=find substring in given substring\n"+
	                          "4=display  given string reverse\n"+
	    		              "5=the given string display  reverse formate string\n"+
	                          "6=both string same or not\n") ;
		 do{
	       
	       System.out.println("enter your choice...");
	        String name = sc.nextLine();
	    switch (inputs)
	    
	    case 1 :
	    	Scanner abs=new Scanner(System.in);
	    	   String a = abs.nextLine();	
	    	 System.out.println(a.length());
	    	   break;
	     case 2:
	    	 String B = sc.nextLine();
	    	
             System.out.println( B.toLowerCase());
             break;
	     case 3:
	    	 String c = sc.nextLine();	
	    	 c.	substring(sc.nextInt());
	    	 System.out.println(c);
	    	 break;
	      case 4:
	    	  String d = sc.nextLine();	
	                int i;
	                for(i=d.length();i>=0;i++) {
	                	System.out.println(d.charAt(i));
	                }
	    	  break;
	   
	      case 5:
	    	String[] string = new String [sc.nextInt()];   
	    	sc.nextLine();   
	    	 break;
	      case 6:
	    	break;
	    
	      case 7:
		    System.exit(0) ;   
	      
          default :
	       System.out.println("enter correct choice");
            break;
            }while(inputs!=7);
		 }
}
	

